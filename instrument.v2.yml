name: jafax
id: jafax
version: 1.0.0
runOrder: 600

actions:
  run:
    commands:
      mine-maven:
        id: mine-maven
        dir: ${instrumentPath}
        command: >-
          java -jar jafax.jar "${repo}" -OL

  summary:
    summaryMdFile: results/summary.md
    summaryHtmlFile: results/summary.html
    commands:
      generate-summary:
        id: generate-summary
        dir: ${instrumentPath}
        command:
          win: py -3 jafax-summary.py "${instrumentPath}/results"
          unix: python3 jafax-summary.py "${instrumentPath}/results"

  pack:
    with:
      locations:
        - source: ${instrumentDir}/results
          destination: /results
