name: jafax
run: once

results:
  - dir: ${instrument}/results

commands:
  - name: Mine Maven
    win: java -Xmx${max-heap} -jar jafax.jar "${repo}" -OL
    unix: java -Xmx${max-heap} -jar jafax.jar "${repo}" -OL

parameters:
  max-heap: 8g
